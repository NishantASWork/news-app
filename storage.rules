rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Article images: read for all, write for authenticated
    match /articles/{fileName} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
